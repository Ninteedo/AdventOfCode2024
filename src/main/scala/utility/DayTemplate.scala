package utility

class DayTemplate extends IDay {
  override def execute(input: String): (Any, Any) = {
    (part1(), part2())
  }

  private def part1() = {
    incomplete
  }

  private def part2() = {
    incomplete
  }
}
